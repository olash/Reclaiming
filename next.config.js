/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow the static HTML pages to be served from the project root via Next.js
  // while API routes live under app/api/
};

module.exports = nextConfig;
